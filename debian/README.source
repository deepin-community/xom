xom for Debian
==============

The upstream supplied source package contains binary jar files and
classes, which are excluded when repackaged.

The upstream tarball does not contain a proper pom.xml, thus it has to be
downloaded from Maven Central for each new upstream release:

$ make -f debian/rules get-orig-pom

 -- Andrius Merkys <merkys@debian.org>  Wed, 05 Jan 2022 01:38:58 -0500
